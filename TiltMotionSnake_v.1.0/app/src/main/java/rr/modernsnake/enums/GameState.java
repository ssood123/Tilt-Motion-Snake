package rr.modernsnake.enums;

public enum GameState { // States of the game. This where we will add more screens and options.
    Start,
    Ready,
    Running,
    Lost,
    Lost2,
    Draw,
    Over
}